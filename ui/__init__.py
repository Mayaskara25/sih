"""UI package -- desktop front-end over pipeline/run_pipeline.py (D35).

See docs/ui_plan.md. Nothing here re-implements detection science: the GUI
calls run_pipeline and renders what it wrote (GeoJSON, mask TIFF, manifest).
"""
